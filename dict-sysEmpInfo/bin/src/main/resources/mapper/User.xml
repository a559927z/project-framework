<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ks.dao.UserDao">

	<resultMap id="userMap" type="com.ks.dto.UserDto">
		<id property="id" column="user_id" />
		<result property="userId" column="user_id" />
		<result property="userName" column="user_name" />
		<result property="userNameCh" column="user_name_ch" />
		<result property="customerId" column="customer_id" />
		<result property="userKey" column="user_key" />
		<result property="password" column="password" />
		<result property="note" column="note" />
		<result property="createUserId" column="create_user_id" />
		<result property="modifyUserId" column="modify_user_id" />
		<result property="createTime" column="create_time" />
		<result property="modifyTime" column="modify_time" />
	</resultMap>

	<select id="findUserById" resultMap="userMap">
		SELECT
		du.user_id,
		du.customer_id,
		du.emp_id,
		du.user_key,
		du.user_name,
		du.user_name_ch
		FROM
		dict_dim_user du
		WHERE du.user_id = #{userId} AND du.customer_id =
		#{customerId}
		and du.is_lock = 0
	</select>

	<select id="testSql" resultType="java.lang.String">select 1</select>


</mapper>